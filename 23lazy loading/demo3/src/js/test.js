// import $ from 'jquery'
console.log('test.js 文件被成功加载了~')
export function add(x, y) {
  return x + y;
}

export function multi(x, y) {
  return x - y;
}

// eslint-disable-next-line
// console.log($)